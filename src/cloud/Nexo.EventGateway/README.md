# Nexo.EventGateway (cloud · .NET) — WIP

Consume Eventos canónicos (`evt.*`) y los **entrega a HEXA**: webhooks firmados (HMAC por empresa) + reintentos + idempotencia, y expone la **API de lectura** para embeber/consultar.

Contratos: ver [`HEXA-INTEGRATION.md`](../../../HEXA-INTEGRATION.md) §4.3 (webhooks) y §4.4 (API de lectura).
