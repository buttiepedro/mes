# Copiá este archivo a `.env` (no se commitea) y ajustá si hace falta.
# Credenciales SOLO para el entorno local de pruebas (docker-compose).

POSTGRES_USER=nexo
POSTGRES_PASSWORD=nexo

MINIO_ROOT_USER=nexo
MINIO_ROOT_PASSWORD=nexo12345
